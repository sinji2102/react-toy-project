import styled from "styled-components";

function ListPage() {
  return <ListPageWrapper>테스트입니다~~ </ListPageWrapper>;
}

export default ListPage;

const ListPageWrapper = styled.section`
  display: flex;

  margin: 3rem;
`;
