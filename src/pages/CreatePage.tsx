import styled from "styled-components";

function CreatePage() {
  return <CreatePageWrapper>노트 생성 페이지입니다~~</CreatePageWrapper>;
}

export default CreatePage;

const CreatePageWrapper = styled.section`
  display: flex;

  margin: 3rem;
`;
